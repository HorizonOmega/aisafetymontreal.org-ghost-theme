<div class="cover gh-outer">
    <div class="cover-content gh-inner">
        {{#if @site.icon}}
            <div class="cover-icon">
                <img class="cover-icon-image" src="{{@site.icon}}" alt="{{@site.title}}">
            </div>
        {{/if}}

        <div class="cover-description">
            <span class="lang-en">Montréal's AI safety ecosystem, in one place.</span>
            <span class="lang-fr">L'écosystème de sûreté de l'IA à Montréal, au même endroit.</span>
        </div>
        <div class="cover-subtitle">
            <span class="lang-en">Monthly newsletter · Events · Organization directory</span>
            <span class="lang-fr">Infolettre mensuelle · Événements · Répertoire d'organisations</span>
        </div>
    </div>
</div>
